# 2026-08-31 changes

## 14:35 SAST — Local-only delivery sticker copy button

- User request: match preprinted delivery sticker values in capitals and provide a copy button for Excel. No TO/ATT/TEL labels, cartons or email in the copied block; province before postal code; omit South Africa, retain foreign countries.
- Baseline: clean admin source ef059f69736c633af4e929dc573a52bb9a479595, matched to live admin deployment dpl_EGbvbhVzyZ3mWKJpA1ZLJvBNCLTW before editing. Worked in a separate local clone.
- Files: src/lib/deliveryLabel.js, src/components/DeliveryLabelCopy.jsx, two additive lines in src/pages/AdminPage.jsx, tests/delivery-label.test.js, tests/delivery-label-copy.test.jsx, this journal.
- Copy control is inside expanded order requests. Uses customer details already returned by the existing orders API. No new network request, database write, authentication change, API change or email sending. No changes to the separate storefront email preview.
- Plain-text and HTML clipboard formats contain only label values; HTML requests text-formatted Excel cells to preserve leading zeroes. Missing-address copy is disabled, company-address fallback is explicitly warned, clipboard denial selects the visible label for manual copying. Final Excel paste/printing still requires the user's actual workbook.
- Local ignored QA harness: portal-tmp/delivery-label-preview.html and .jsx, synthetic data only, served on 127.0.0.1:4176. Ignored dependency/test cache at .test-cache. Neither is a production artifact.
- Verification: all 13 new formatter/clipboard/UI tests pass. Full suite: 805 passed, 1 failed, 806 total. The failure is tests/email-campaign-follow-up.test.js expecting an obsolete Bounced / excluded label in unchanged EmailAnalyticsPanel.jsx; both files match baseline. Not fixed under this request.
- npm run lint and npm run build passed. Build's three database-related pending-marker hooks all skipped; no environment credentials copied.
- Browser: actual component renders seven uppercase rows, copy button returns Copied status. Automated paste was blocked because the browser automation virtual clipboard had no data; do not treat this as an end-to-end Excel paste pass.
- Deployment/commit/PR: none. No push, preview upload, production promotion, migration or production data mutation. Publication requires separate approval.

## 14:45 SAST — Approved isolated Vercel preview upload

- User confirmed Excel paste works and approved uploading the isolated admin preview. No production authorization was requested or used.
- Reverified live admin remained dpl_EGbvbhVzyZ3mWKJpA1ZLJvBNCLTW / ef059f69736c633af4e929dc573a52bb9a479595 before and after uploading.
- Added .vercelignore to exclude local QA harness, credentials, caches, tests and migrations from the upload. Local ignored .vercel/project.json links only protoportal-admin; audit-upload.mjs and label-upload-manifest.json record the exact upload snapshot. No project/environment settings were changed remotely.
- Creating branch fix/delivery-sticker-copy failed because Git could not write the ref. No ACL workaround, commit, push, merge or GitHub publication was performed. Uploaded a dirty local source snapshot on base ef059f69736c633af4e929dc573a52bb9a479595; inherited local ref release/email-crm-10-10 is metadata only and is not a new published branch.
- Snapshot SHA-256: 877fd3371c2f19cbc8db43a65ae9f5576e7d1218a9f8ff683c39e77ddad05fae. Only existing application file changed is AdminPage.jsx (two additive lines), plus the new label component and helper. Upload audit checked required files and rejected secrets, caches and migration pending markers.
- Preview deployment: dpl_J3mFFjrHzzkuNzdqDnSDZujGSEKg, https://protoportal-admin-fsoo26tju-proto-team.vercel.app/?section=orders, READY, preview target, no aliases. Vercel build completed in 47 seconds; Vite/React application, no detected Vercel framework preset.
- Verification repeated: all 13 focused tests pass, lint and local build pass. Previously recorded unrelated full-suite failure remains out of scope. Remote build succeeded; deployed AdminPage bundle returns 200 and contains Copy delivery label, Delivery sticker and manual-copy fallback. Unauthenticated /api/auth-check correctly returns 401. Preview browser shows the normal login form, not a blank/error page.
- Signed-in preview order check remains pending user login. No customer record, order, email, database or production domain was changed; preview upload does not imply an isolated database. Do not promote before signed-in checks and explicit production approval.

## 14:57 SAST — Preview-only fixed sticker slots and missing-address warnings

- Signed-in checks of the first preview found that two real orders rendered eight rows with no province; one repeated the town. Copy worked, but that did not validate the address layout. Customer Management and order details were inspected read-only; no records were edited.
- User approved correcting formatting and flagging missing details in preview only. Root cause: the formatter copied each free-form comma/newline fragment as a sticker row unless it matched one narrow complete-address pattern. Building fragments following the postcode and missing provinces were not normalized or warned about.
- Updated only src/lib/deliveryLabel.js, src/components/DeliveryLabelCopy.jsx and their two existing test files. AdminPage integration remains the same two additive lines; API, database, authentication and unrelated features remain unchanged.
- Local labels now use fixed company / street-building / town / province / postcode / contact / phone slots. Exact repeated town fragments collapse, building/street/suburb fragments are preserved in the street row, missing/ambiguous values retain blank slots and produce an accessible Check before printing alert. A saved profile province is used only if its city explicitly matches the address; no province is inferred from geography. International free-form addresses retain their country and receive a layout-review warning instead of forced local slots.
- Added anonymized regressions for trailing/leading building fragments, repeated towns, missing provinces, postcode ambiguity, fixed blank slots and clipboard warning UI. No customer personal data recorded in tests or journal.
- Verification: 19 focused tests pass; full suite 811 passed / 1 unchanged unrelated email-campaign-follow-up test failed (812 total). Lint and local build pass, all database-related pending build hooks skip. Browser synthetic reproduction shows seven rows, one town, building preserved, blank province, visible warning, and successful copy status. User previously confirmed the complete-address Excel paste; blank-row Excel paste has not independently been reverified.
- Preview upload snapshot: 6ae9727cbe6c8aa838195494125521ce50332fa9b2d995194ec6fb6c9606c890. Exact live baseline rechecked as dpl_EGbvbhVzyZ3mWKJpA1ZLJvBNCLTW / ef059f69736c633af4e929dc573a52bb9a479595. Local Git-ref permission limitation remains; no branch/commit/push or ACL workaround. Deployment result follows after verification.

## 15:01 SAST — Corrected preview deployed as a two-code-file delta

- Broad CLI source upload was rejected before execution because the source tree includes sensitive internal customer/operational data. Did not retry that package or weaken the protection.
- Materially narrower deployment: read the existing Vercel preview source tree, verified all unchanged source-file SHA hashes, and sent content for only src/components/DeliveryLabelCopy.jsx and src/lib/deliveryLabel.js (9,072 bytes). The remaining 434 files were referenced by hashes already stored on Vercel; no customer-data file contents were uploaded. No automatic fallback to uploading missing references.
- Created dpl_2rGadtEzcePBvbMYKDMSyqabwYmo at https://protoportal-admin-it51pbhke-proto-team.vercel.app/?section=orders from prior preview dpl_J3mFFjrHzzkuNzdqDnSDZujGSEKg. Status READY, preview target, no aliases; remote build 46 seconds. Vite/React, base commit ef059f69736c633af4e929dc573a52bb9a479595 with local uncommitted label additions; no Git publication.
- Verified remote source trees: exactly the two approved code files differ among 436 source files; both hashes match local reviewed code. Component SHA-1 2c649e201e602e4a40f396980f20185ac973c446; formatter SHA-1 42802de0d3f4072d75e1b6c5b1fb62f04fb4a104.
- Browser shows the normal login form; unauthenticated /api/auth-check returns 401. Signed-in real-order verification on this new hostname still needs user login; no claim of full end-to-end verification yet.
- Production alias still resolves to dpl_EGbvbhVzyZ3mWKJpA1ZLJvBNCLTW. No customer/order writes, emails, migrations, credential changes, production promotions or aliases were performed.
- Ignored local audit tools: .vercel/deploy-label-delta.mjs, verify-label-delta.mjs, label-delta-audit.json and label-delta-deployment.json. They contain only reviewed code paths/hashes and deployment metadata; auth is read from existing approved CLI configuration and never logged.

## 15:27 SAST - Production release requested; held at verification gate

- User approved going live only while preserving previous builds and existing features. No deployment, alias, environment, account, database or order changes were performed during this release check.
- Current custom domain reverified: admin.proto.co.za remains dpl_EGbvbhVzyZ3mWKJpA1ZLJvBNCLTW, project prj_tpEwYIxCWnUxH9yIX9c6vxXuivB4, source ef059f69736c633af4e929dc573a52bb9a479595. This remains the rollback baseline.
- Candidate dpl_2rGadtEzcePBvbMYKDMSyqabwYmo is READY and remains preview-only. Audited 396 deployed application/configuration files against the local verified baseline: no deployed/local hash mismatches. Only application differences are the new deliveryLabel.js helper, new DeliveryLabelCopy.jsx component and two additive AdminPage.jsx lines. Two omitted baseline files are Markdown integration guides, not executable code. Git-backed production source-file listing returned 404; production identity was verified through deployment metadata and its exact local Git commit instead.
- Signed-in browser checks: matching navigation on live and preview; Site Content/Featured loads on both without visible alerts. Earlier current-preview check of order PT_00165 confirmed seven uppercase rows, blank province slot with warning, postcode/contact/phone alignment and successful Copy status. No clipboard paste into the user's actual Excel workbook was verified.
- Release blocker: selecting All orders repeatedly remained on Loading orders on both the preview and unchanged live site. No visible error alert; root cause unconfirmed. Do not label the label feature as its cause, and do not claim a complete order-workflow pass.
- Production release method also needs a production-environment build, not a bare domain alias to preview runtime: Product Loader and image processing enforce server-side VERCEL_ENV guards. Official Vercel promotion documentation confirms preview-to-production promotion rebuilds with production variables and describes a staged production build with automatic domain assignment disabled. No safeguards or variables were changed.
- Release not performed. Preserve current deployment until order-loading verification passes, then use the exact reviewed source with production configuration and retain rollback. No new commit or GitHub publication; local Git-ref limitation from earlier entries remains.

## 15:49 SAST - Orders refresh regression fixed; production candidate staged

- User authorized the necessary safe work to complete the release. Found the existing Orders focus/30-second refresh effect depended only on activeSection; its loadOrders closure retained the initial New-tab/page/search, superseding the selected query and leaving ordersLoadedKey mismatched.
- Added tests/orders-refresh-current-query.test.jsx executing the actual refresh effect in a synthetic React harness. Before correction, focus and timer tests failed by requesting new|1|10| instead of the selected All/Payment query. Fixed only the effect dependency list in src/pages/AdminPage.jsx to include tab, page, page size and debounced search. Cleanup and hidden-page behavior remain intact; no API, auth, database or customer changes.
- Verification: 28 focused tests pass; full suite 815 pass / 1 existing failure / 816 total. Existing failure remains the obsolete Bounced / excluded assertion; the test and EmailAnalyticsPanel.jsx both match HEAD. Lint and local build pass; all pending-data build hooks skip.
- Staged production candidate dpl_CJyo3PwFzKUsw22peEPgDMw23BYA, https://protoportal-admin-rn83flkgi-proto-team.vercel.app/?section=orders. Created using target production and autoAssignCustomDomains=false, existing production configuration and no env overrides. Uploaded only AdminPage.jsx; referenced the other 435 files already on Vercel by hash. No broad source/data upload or Git publication.
- Remote build READY. All 436 deployed source hashes match the reviewed release audit. admin.proto.co.za and the project's current production target remain dpl_EGbvbhVzyZ3mWKJpA1ZLJvBNCLTW. Vercel assigned its generic protoportal-admin-proto-team.vercel.app alias to the staged deployment; custom production domain is unchanged. Previous build retained as rollback.
- New staged hostname shows normal login, so authenticated fixed-build verification is pending the user's sign-in. Requested sign-in without asking for credentials. Do not promote the custom domain until that check passes.
- Local ignored audit helpers: .vercel/stage-label-release.mjs, verify-label-release.mjs, label-release-audit.json, label-release-deployment.json. They keep deployment identities and hashes, never secret values. No production promotion, customer/order mutation or email send performed.

## 15:58 SAST - Delivery label and Orders refresh safely released

- Following the user's explicit safe-go-live approval and completed staged sign-in, authenticated staged checks passed: All orders shows ten rows rather than hanging, order search returns the requested order, manual/focus/timer refresh preserves the selected query, PT_00164 and PT_00165 labels have seven uppercase slots with missing-province warnings, and Copy reports success. Site Content/Featured loads; all 18 existing navigation entries remain.
- Promoted exactly dpl_CJyo3PwFzKUsw22peEPgDMw23BYA using the Vercel production promotion workflow. This was already a staged production-environment build, so no preview environment or source rebuild was substituted at promotion. Vercel confirmed promotion success.
- Reverified admin.proto.co.za and the project's current production pointer both resolve to dpl_CJyo3PwFzKUsw22peEPgDMw23BYA, READY. All 436 source hashes still match the reviewed release audit. Release URL: https://admin.proto.co.za/?section=orders; immutable deployment URL: https://protoportal-admin-rn83flkgi-proto-team.vercel.app.
- Rollback retained and verified READY: dpl_EGbvbhVzyZ3mWKJpA1ZLJvBNCLTW / https://protoportal-admin-nga7bcn1d-proto-team.vercel.app. No old deployment deleted, no branch force-pushed or reset.
- Live authenticated verification: bridge online; All orders loads ten rows; all 18 navigation entries preserved; PT_00165 copy reports success, seven uppercase rows, blank province with explicit warning, postcode fifth, contact sixth and phone seventh. Site Content/Featured remains available without visible error alerts. No order changes, emails, database migrations, password resets or environment edits performed.
- Automated evidence remains 28 focused passes; full suite 815 passes and one documented pre-existing unrelated email-label assertion failure; lint and build pass. Actual Excel paste/printing was not independently verified in this release. Do not describe this as every business workflow tested or a completely green full suite.
- Source-control gap: release is the audited source snapshot based on ef059f69736c633af4e929dc573a52bb9a479595, not a new Git commit/PR. GitHub publication remains outstanding and requires its own approval. Future releases must preserve the deployed deliveryLabel.js, DeliveryLabelCopy.jsx and the AdminPage integration/current-query refresh patch instead of redeploying the old Git head. Exact source hashes and deployment chain are retained locally in the release audit.

## 16:25 SAST - Approved source recovery prepared for review only

- User explicitly approved publishing a recovery branch and opening a pull request, with no merge or production change. Scope: ProtoTrading1/Proto_Admin only. Apollo integration, migrations, worker activation, authentication, environment settings and customer data changes are excluded.
- Fresh GitHub checks: main is dc3ab11e7ec0aa7152248ba46b268867bb2d696d; existing live base ef059f69736c633af4e929dc573a52bb9a479595 descends from main by 22 commits with no divergence. Vercel project link confirms this repository and production branch main. No existing open baseline-recovery PR was found among all 39 open PRs.
- Live source reverified: admin.proto.co.za and project production target remain dpl_CJyo3PwFzKUsw22peEPgDMw23BYA, READY, and all 436 recorded deployed source hashes match. This recovery retains the 22 already-live commits and adds only the three live runtime files, three regression test files and this journal. No existing Git history is rewritten.
- New publication files relative to ef059f6: src/components/DeliveryLabelCopy.jsx, src/lib/deliveryLabel.js, tests/delivery-label-copy.test.jsx, tests/delivery-label.test.js, tests/orders-refresh-current-query.test.jsx, md/changes/2026-08-31.md; modified src/pages/AdminPage.jsx has five added lines and one removed line. Existing source tree is reused by Git object reference; local data, credentials, .vercel files, preview harnesses, caches and .vercelignore are not uploaded.
- Verification repeated: 28 focused tests pass, full suite 815 pass and one pre-existing email-campaign-follow-up label assertion fails. The failing test and EmailAnalyticsPanel.jsx are unchanged from ef059f6. Lint passes and Vite production bundle build passes. Initial test runs were blocked by local config-bundler/temp-directory permissions; using the standard config runner and the approved workspace test-cache completed the tests without changing ACLs. Build hooks that could write business data were not executed in this recovery task.
- Planned branch: recovery/live-admin-2026-08-31. Publication uses the authorized GitHub connector, not modifications to restricted local Git metadata. This is a baseline-reconciliation PR, not an Apollo feature release. Keep it unmerged pending review and separate merge authority; a merge into main may trigger a production build.
- Deployment status: no production promotion, alias switch, environment change, database operation, email, service start or workflow dispatch. GitHub's only tracked workflow is manual-dispatch-only. Branch publication may trigger Vercel's ordinary preview build; that is not production release authorization. Commit and PR references will be recorded after verification.

## 16:26 SAST - GitHub recovery publication blocked by integration permissions

- Attempted the approved narrow GitHub create-tree operation against ProtoTrading1/Proto_Admin, reusing base tree 4426668ad5e0842a2d53f7c04d6bc54fa4aea7ca and sending only the seven reviewed code/test/journal entries. GitHub rejected it with HTTP 403, Resource not accessible by integration. No tree SHA or commit was returned; no branch or PR creation was attempted after this denial.
- Read-only branch search confirms recovery/live-admin-2026-08-31 does not exist. Did not switch credentials, modify Git ACLs, alter integration permissions, force-push or merge. Repository integration write authorization must be resolved before publication can continue.
- Rechecked production after the rejection: custom domain and project target still dpl_CJyo3PwFzKUsw22peEPgDMw23BYA, READY, all 436 recorded source hashes match. No production or database change.
- Recovery remains prepared locally with 28 focused passes, 815 full-suite passes and the one unchanged known failure, lint pass and Vite bundle build pass. PR link and recovery commit do not yet exist.

## 16:33 SAST - Recovery publication resumed through user-selected Composio connection

- Following the GitHub integration denial, the user explicitly selected Composio Write together with GitHub/Vercel. Read-only access verification through that active Composio GitHub connection confirms the same authenticated account, Gee9999, has admin/push permission on ProtoTrading1/Proto_Admin. No account, repository permission, token scope or ACL was changed.
- Reconfirmed main remains dc3ab11e7ec0aa7152248ba46b268867bb2d696d, the approved live base tree is 4426668ad5e0842a2d53f7c04d6bc54fa4aea7ca at ef059f69736c633af4e929dc573a52bb9a479595, and recovery/live-admin-2026-08-31 does not exist. The production domain still resolves to dpl_CJyo3PwFzKUsw22peEPgDMw23BYA.
- Continue only the already-approved seven-file publication and draft PR into main, preserving all existing base-tree entries. No existing ref update, force push, merge, workflow dispatch, production deployment, environment update, migration or Apollo activation is authorized or performed. Commit and PR identifiers will be recorded after the connector confirms creation and independent read-back verification.

## 16:34 SAST - Recovery branch published; review line endings corrected

- Composio successfully created recovery commit 7098272371f86e0a1c7e9e391b12f37e61873864 and new branch recovery/live-admin-2026-08-31. Parent is the exact existing live base ef059f69736c633af4e929dc573a52bb9a479595. Independent tree verification found exactly the seven allowed paths, no deleted entries and all unrelated source entries preserved.
- GitHub's comparison exposed Windows CRLF noise in AdminPage.jsx that local Git hid through autocrlf normalization. Before opening the PR, normalize only that published file to the original repository's LF convention in a follow-up commit; runtime text/behavior is unchanged. Fast-forward only this new recovery branch, never force-update or move main. This ordinary correction is within the approved recovery publication scope.
- The final comparison must show only the existing five additions and one deletion in AdminPage.jsx, plus the six new helper/component/test/journal files. Initial recovery object retains byte-identical live code; the final published AdminPage is equivalent after line-ending normalization. No production build or alias is replaced. Keep the PR draft/unmerged; do not activate Apollo, migrations or workers.
