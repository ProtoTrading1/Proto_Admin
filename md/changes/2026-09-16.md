# Change journal — 2026-09-16

## 01:53 SAST — Apollo validation and preview preparation

- Refined the Apollo order-flow UI test fixture to provide the insight it
  asserts, and updated the API regression expectation to the explicit
  `orders` insight kind and payment-status disclosure. No production behavior
  changed in this test correction.
- Updated `md/apollo-release-gates.md` with current release evidence. Apollo
  admin tests pass **132/132**; the storefront test suite passes **389/389**;
  both Vite production builds and targeted Apollo ESLint checks pass. The full
  admin suite passes **945/946**, with the remaining failure isolated to the
  unrelated email-campaign follow-up label contract.
- Read-only Supabase check confirms the isolated Apollo database has the four
  Apollo tables and the live parent database has none. The branch remains
  `MIGRATIONS_FAILED`; a narrow branch-log query returns HTTP 403. No database
  migration or data write was attempted.
- Read-only Vercel metadata confirms Apollo-specific variables are scoped to
  the admin preview branch. Secret values were not decrypted. Preview
  deployment will remain separate from production.
- A current local admin build was produced with `npx vite build
  --configLoader runner`; storefront `npm run build` also passed. No deploy or
  push has yet been made in this journal entry.
- The local preview branch and remote preview branch had diverged Apollo
  implementations. Merge conflicts were reviewed; the current local
  isolated-activity implementation and all 132 tested Apollo cases were kept
  rather than reverting to portal-visit-only activity. Both histories remain
  in the merge ancestry; no force-push was used. The isolated preview
  deployment is the next step; production remains unchanged.
- Files: Apollo admin API/reporting/insight/UI code, focused tests,
  `vercel.json`, release gates, and this journal. The unrelated Instore image
  test edit remains unstaged and outside this release.
- Deployment status: local candidate only; production unchanged.
- Commit/PR: `3445317`; push to the existing non-production branch was
  attempted after explicit network permission, but git produced no remote
  response and was stopped after a bounded wait. No force-push or alternate
  remote was attempted; the branch remains local ahead of origin.
- Follow-up check: the dedicated `vercel_mcp` connector remains
  `initializing`, and its wait endpoint returned HTTP 504; no deployment was
  triggered. The exact preview connection link remains the required external
  action before the candidate can be published and browser-tested.
- Subsequent status check still reports that connector as `initializing`; no
  preview deployment was attempted through an unapproved or production path.
- A second bounded `git push` retry with terminal prompts disabled also
  produced no remote response and was stopped. The local commit is preserved;
  no force-push, branch deletion, alias change, or production action occurred.

