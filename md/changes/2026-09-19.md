# 2026-09-19

## 00:07 SAST — Diagnose and repair isolated migration replay

- Corrected earlier diagnosis: `20260724192514` is the last successful migration. Local PostgreSQL replay proves `20260727184948` fails first because `public.main_categories` is absent.
- Captured production migration SQL and schema definitions read-only, without product/customer rows, under sibling `work/migration-replay`.
- Added a local PGlite replay harness with pinned dependency. Unmodified historical replay reproduces SQLSTATE 42P01. Supplied missing schema definitions, columns, constraints and functions for test-only baseline; all 18 historical migrations then pass with 36 synthetic rows required by the old VAT repair. Verified 8.26 -> 9.50 and removed synthetic rows locally.
- Both Instore migrations apply twice successfully. SQL tests pass for stage, positive confirmed stock, to-order null quantity, optimistic version protection, recycle/restore, blocked approval, private-role permissions, and five expected audit events.
- Created authorized no-data test branch `instore-replay-repair-20260919`, project `aovcrilrgistrdcgtdgl`, branch ID `c6de8564-64fa-4b23-a6c6-45ee9110068b`. Added `test_only_missing_schema_prerequisites`, seeded 36 explicit REPLAY-* synthetic rows and requested rebase. Cloud replay verification pending at this entry.
- Production schema, data, migration ledger and deployment remain unchanged. Test-only prerequisite migration must NEVER be merged into production.
- Review application PR remains #239. This entry does not claim browser E2E, Storage API or live Positill verification.

## 00:10 SAST — Cloud SQL verification

- Added the missing pg_trgm extension in the disposable branch. Automatic rebase progressed through preserve_website_selling_units, then failed. Executed the exact remaining production migration SQL individually, with explicit test_replay_* ledger entries; all succeeded. Supabase branch pipeline status still reports MIGRATIONS_FAILED; do not describe this as a clean automatic clone or merge its ledger into production.
- Applied both Instore review migrations to the test branch. Cloud SQL verifies stage, stock_available, to_order, recycle, restore and blocked approval. Lifecycle exercise rolled back; one TEST-001 archived synthetic review fixture remains, with a placeholder path and no uploaded image yet.
- Verified 36/36 synthetic VAT fixtures reached 9.50, then removed precisely those REPLAY-* fixture rows and repair rows. They were generated only for this test and contain no real product data.
- All three Instore tables have RLS enabled and deny anon SELECT; bucket is private with 2,097,152-byte limit.
- Preview connection remains pending: existing Preview configuration names deleted project zbxvcdkcarrgtmdhwmdm. The new branch needs its own server-only key and URL/ref configuration before browser testing.

## Preview connection repair

- With explicit user approval, saved the replacement test project's server key in STOCK_SUPABASE_KEY, restricted to Preview and codex/instore-admin-review-20260918. Updated STOCK_SUPABASE_URL and INSTORE_ADMIN_TEST_PROJECT_REF for aovcrilrgistrdcgtdgl in the same branch scope. No credentials recorded here.
- Vercel shows no Preview STOCK_SUPABASE_POOLER_URL override (only Development and Production entries).
- Rechecked admin.proto.co.za: still deployment dpl_3PhNAoQGXsn6ZgqUXcPns5y2U8k1, matching the protected production fingerprint.
- Verified source deployment dpl_22HEk1ekzjNkNsJ8mhBq5Ai3kP7H is Ready, Preview, commit 2580a23c8438462418901bf4dddfb41252954a6c. Requested Preview-only redeploy using latest settings without build cache. Completion and browser E2E remain pending.
- Reran local baseline SQL harness: all 18 historical migrations, repeat Instore migration applications and lifecycle/security assertions pass.
- Rebuild reached Ready: dpl_GsWM1WZxMvAKNcaLYzx2cgy1c4xR, https://protoportal-admin-fufa3nuq6-proto-team.vercel.app, source 2580a23c8438462418901bf4dddfb41252954a6c. Immutable URL renders the admin login form. Existing branch alias renders a blank body after reload, fresh navigation and a fresh tab; no captured console error. Authenticated E2E is NOT passed; fresh-preview sign-in needed. No production promotion.

## Signed-in preview testing

- Authenticated immutable preview loads Instore navigation and archived TEST-001 from isolated database.
- UI rejects Stock available with quantity 0. Saving quantity 3 succeeded (database version 2); To order succeeded and cleared confirmed_qty to NULL (version 3). These changes affect TEST-001 only.
- Folder chooser test attempted TEST-002.png, but observed completion was 30 mug images, not the intended single fixture. TEST-002 is absent. Do not claim controlled single-image staging passed; investigate folder selection/batch provenance before repeating uploads. No items were published or deleted.
- All 30 rendered table images report complete with positive naturalWidth. All 30 corresponding product records report positill_exact_lookup_unavailable with missing price/unit. Staging displayed only a generic busy label during the long batch, then 30 images staged.
- Approval remains disabled pending publishing integration. Release remains blocked by Positill enrichment and publication contract. Production unchanged.

## 00:36 SAST — Intake review and diagnostic fixes (local only)

- Reverified production Vercel overview: admin.proto.co.za still maps to dpl_3PhNAoQGXsn6ZgqUXcPns5y2U8k1.
- Allowed change scope: api/instore-admin.js, src/lib/instoreAdminApi.js, src/components/InstoreProductsPanel.jsx, their two focused test files and this journal. Preserved earlier local API payload-limit and timeout changes already present in remote review branch.
- Distinguish Positill exact-code misses, code mismatch, timeouts and connection failures without exposing upstream error strings. This improves diagnosis; it does NOT yet prove or repair the live source failure.
- Added explicit selected filename review before uploads, separate ordinary image-file picker and per-file batch progress. New selection uses a new batch ID; server digest checks preserve retry idempotency.
- Await response JSON within request timeout try/finally so body parsing remains covered by timeout.
- Build and lint pass. Focused API/UI tests 26/26 pass using Vitest --configLoader runner, avoiding the Windows esbuild config-loader permissions issue. Existing React act cleanup warnings remain.
- No code published or deployment changed in this step; preview still runs 2580a23. Deployed diagnosis and end-to-end retest remain required.

## Publication attempt after user approval

- Rechecked production fingerprint unchanged and PR #239 head 2580a23c8438462418901bf4dddfb41252954a6c.
- GitHub connector can read the repository but all five scoped file updates were rejected with HTTP 403 Resource not accessible by integration. No remote files were changed and no new Preview build was created.
- Local implementation, journal and passing 39-test verification remain preserved. Publication requires restoring repository Contents write access for the connected integration; no permission changes attempted.

## GitHub connector installation

- After explicit action-time approval, installed ChatGPT Codex Connector on ProtoTrading1 with Only select repositories: Proto_Admin. Installation 162872199 verified on GitHub settings.
- Granted app-declared read checks/status/metadata and read/write code, Actions, workflows, issues and pull requests; user email read permission disclosed before authorization. No access added to ProtoMainSite, and Claude/Vercel installations left unchanged.
- Repository write operation and Preview deployment still require verification; installation alone is not proof that connector token permissions refreshed.

## Verified publication after installation

- GitHub Contents writes succeeded for all five scoped implementation/test files. Review branch code head c0f3bfbb777aadf7ecfd8b16a0346129e5ffccc1; PR #239 remains unmerged. Production fingerprint rechecked unchanged before publishing.
- Preview build and deployed Positill diagnosis pending. No production changes.

