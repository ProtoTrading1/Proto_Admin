# 2026-09-03

## Apollo source coverage and privacy foundation

- Added one permission-aware catalogue for Apollo's approved read-only sources, their Admin destination, privacy boundary, delivery mode and connection status.
- Added an authenticated source-coverage endpoint so Apollo can explain what it can and cannot currently check.
- Expanded question routing for catalogue/archive, customer totals, communications, site content, Product Loader audit and fulfillment workflow questions.
- Added server-side aggregate snapshots for those business areas. Raw customer names, contact details, campaign subjects, filenames, SKUs and audit text are removed before the Codex CLI boundary.
- Kept identity-bearing product and buyer answers deterministic inside authenticated Admin rather than sending identities to the model worker.
- Marked Buying, aggregate Pricing and Team-access intelligence as planned instead of allowing unsupported answers.
- Added evidence references for the new aggregate metrics and focused worker instructions for each business area.
- Added a visible Apollo coverage control and a website-content shortcut to the Apollo workspace.
- No production deployment, database migration or write-capable Apollo action was added.

## Verification

- Apollo-focused contract, routing, privacy, worker, recovery and UI tests pass.
- ESLint passes.
- Production bundle compilation passes.
- Full regression run: 1,004 passed; one inherited non-Apollo CRM label assertion remains (`Bounced / excluded` versus the current `Bounced` label).
