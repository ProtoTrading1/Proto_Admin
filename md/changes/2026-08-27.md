# Changes — 2026-08-27

## Preview recovery — Featured grid baseline and supplied colour suffixes

- Summary: Prepared a preview from the current `main` baseline, which already
  contains the Featured storefront ordering grid, and extended the controlled
  Product Loader colour vocabulary for the supplied `DPNK`, `DRED`, `LPNK`,
  `LRED`, and `LPUR` filename suffixes. Existing exact-SKU matching, image-slot
  allocation, and all unrelated admin behaviour remain unchanged.
- Files affected: `lib/product-colour-variants.mjs`,
  `tests/product-colour-variants.test.js`, and this journal entry.
- Systems affected: Product Loader filename interpretation for recognised
  colour variants. No database, product, image, price, stock, storefront,
  authentication, environment, or production-alias mutation.
- Verification: Featured-grid and supplied-colour regressions passed (29/29);
  full suite passed 753/754 with the one existing Image Processing Centre
  stale-queue timing failure reproduced unchanged on clean `origin/main`;
  ESLint and the production build passed. Built bundles contain the Featured
  ordering grid and the supplied colour suffix vocabulary. Preview deployment
  and signed-in browser handoff remain pending.
- Deployment status: Preview only; production remains on deployment
  `dpl_BHxw3tGvGLx3NrgELBExGabaAA3s`. Clean current-`main` Featured-grid
  preview: `dpl_EJvG9rzNdprx3bGpew6Y8keajPr9`
  (`protoportal-admin-jdab0o45p-proto-team.vercel.app`). Supplied-colour suffix
  preview: `dpl_HGiog31TqznFzGSqBhuVjxZzVZec`
  (`protoportal-admin-kkxptat6v-proto-team.vercel.app`). Both are READY with no
  production or custom-domain alias. The Featured preview bundle contains the
  ordering grid, Arrange order, Pick products, and drag controls. Signed-in
  checks remain a manual handoff because the preview correctly requires an
  authorised Proto account.
- Commit or PR: Feature preview commit `880e65629666ece584b3f9bc4339048e60c1822f`
  on local branch `preview/restore-grid-and-colour-suffixes-current-main`;
  no public push or pull request yet.
