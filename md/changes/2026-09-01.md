# 1 September 2026

## 10:54 SAST - Apollo reconciled implementation started locally

- Summary: created a clean isolated integration repository from recovery/live-admin-2026-08-31 at acdad7891ef9a83e333203ece37383c4390312ac. User explicitly re-authorized Apollo as a read-only Proto assistant; the prior repository note that Apollo was removed is superseded only for this feature. All other admin, authentication, order, fulfilment and removed-feature constraints remain.
- Initial shell scope: renamed the existing `hermes` navigation label and loading/error text to Apollo, while keeping the route id stable. Added a narrowly scoped preview-write exception for authenticated isolated Apollo analysis enqueue; all other preview writes remain blocked.
- Files: `src/pages/AdminPage.jsx`, `src/components/GroupedSidebar.jsx`, `src/lib/previewWriteGuard.js`, `tests/preview-write-guard.test.js`, `tests/admin-baseline-preservation.test.js`.
- Preservation: tests explicitly retain all 18 current navigation ids, `DeliveryLabelCopy`, and current-query order refresh. Email CRM files and behavior are out of scope and unchanged.
- Verification: first Vitest attempt could not create its sandboxed system-temp directory. Retried with a repository-local `.test-cache`; 7/7 focused shell/preservation tests passed. Dependency install used existing lockfile and ignored lifecycle scripts; package audit reported nine inherited advisories for later review. No dependency upgrade or audit fix was applied.
- Deployment: local work only. No GitHub publication, preview upload, merge, production change, database migration, worker activation, model request, customer-data write or email send.
- Commit/PR: none. Existing recovery PR #238 remains separate, draft and unmodified.

## 10:55 SAST - Reconciled baseline full-suite result

- Ran the entire local Vitest suite from the recovered source with the repository-local temporary directory: 816 passed and one failed across 95 files. This includes the new Apollo shell guard test, so the total is one test above the previously recorded recovery suite.
- The sole failure is the already-known `tests/email-campaign-follow-up.test.js` inherited from the live baseline. Email CRM is explicitly outside the Apollo scope and no email source/test was changed.
- This baseline result is recorded before merging the three Apollo implementation workstreams. Full suite, lint and build will be repeated after integration.

## 11:23 SAST - Apollo conversation, evidence and worker integration verified locally

- Added a bounded read-only Apollo conversation contract: exact questions, at most four short prior turns, approved source planning, client and server rejection of write requests and personal contact details, and no automatic replacement of a saved answer.
- Added authenticated server snapshots for orders, customer attention, search, baskets, backend health and image-processing status. Every source now carries availability, collection time, reporting window, completeness and truncation metadata; unavailable sources remain unavailable rather than becoming zero.
- Added opaque product/category/search references and mandatory evidence citations for model findings. Raw unapproved search terms and customer detail are excluded from the model boundary. Worker input/output size, host, authentication, claim token, lease, timeout, sandbox and no-tool restrictions remain enforced.
- Focused Apollo suite: 156/156 passed. Full recovered-admin suite: 973 passed; the same one inherited email follow-up test remains the only failure and was not edited. ESLint and production build both pass.
- A separate isolated storefront reconciliation now owns customer attention and queue migrations; no migration has been applied from this integration and no worker has been installed or activated.
- Deployment remains unchanged: no GitHub push, Vercel upload, database mutation, production promotion, email send or model request.

## 12:25 SAST - Isolated Apollo previews uploaded and safety-checked

- Admin preview: `dpl_BLmNkyjzcNgjL3Y1grZXkH131FBQ` at `https://protoportal-admin-aroxld872-proto-team.vercel.app`. Vercel build completed successfully. Remote asset inspection confirmed the new Apollo conversation UI, the approved isolated Supabase ref `xicygaamdogfdpzyrlcp`, and no production portal ref. Anonymous job reads and untrusted worker claims both return 401.
- Storefront preview: `dpl_FoEMLNVMHGAjrRdggpcXanfFXvTE` at `https://protoportal-main-cafe7f5oo-proto-team.vercel.app`. Vercel build completed successfully. The preview was uploaded with the isolated public project configuration and a deliberately non-privileged server key so writes fail closed. Browser QA confirmed the storefront loads and the privacy policy contains the signed-in catalogue-attention disclosure, active-time definition, 90-day retention, aggregate Apollo use and read-only boundary.
- Existing isolated gateway answered its health snapshot request with 200. Previously completed genuine CLI job `e8d5dac1-ce27-4f14-8bf8-2c3b3b460f07` remains completed with a saved result. A fresh synthetic question was safely queued as `25a6f3b6-f0aa-46d3-b6cf-ad1a57c9f9cf`.
- The fresh one-off worker did not run: Windows correctly denied this sandbox access to the existing SSH identity and known-hosts files. Their ACLs were not weakened. The local Codex CLI also cannot load a home directory in this sandbox. The new job remains queued and was not claimed, duplicated or fabricated.
- No production deployment, domain alias, production database migration, production environment-variable change, service/timer installation, customer-data write, email send or other production mutation occurred.

## Fresh CLI completion confirmed

- Owner-run one-off worker returned job `a05058dc-62b6-462d-aee1-577574e7c2a1`.
- Read-only gateway verification confirms status `completed`, a saved structured result, a valid Apollo report contract, and no email-address or phone-number pattern in the report.
- The worker remains one-off only. No service or timer was installed or enabled, and production remains unchanged.

## 13:47 SAST - Buyer-ranking question repaired locally

- Fixed the exact question `who bought the most stock online this week ?` so it routes to a dedicated authenticated online-order ranking instead of Backend Health.
- “This week” is Monday 00:00 South Africa time through now. Ranking is deterministic: valid ordered units first, then ex-VAT order value and order count. Cancelled, rejected, voided, failed and deleted requests are excluded.
- The answer is calculated inside authenticated Admin and does not use the Codex worker. Customer IDs, contact names, email addresses, phone numbers and product lines are not returned to the browser or sent to the model.
- Focused routing, calculation, API-contract and UI verification: 45/45 passed. The existing full suite, lint and production build remain as recorded above; production has not been changed.
