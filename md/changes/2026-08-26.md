# Changes — 2026-08-26

## 00:25 SAST — Restore the Unsubscribe chip in the email composer

- Summary: The broadcast composer's "Insert field" bar lost its Unsubscribe
  button when `eebdd2a` reverted `9e5a20c` on 2026-08-18. The revert carried no
  stated reason and nothing since depended on its absence, so the original
  change has been reapplied as-is. The chip inserts `{{unsubscribe}}` (a small
  muted text link) at the cursor in the subject, intro or HTML/banner block;
  `{{unsubscribe_url}}` remains available for a bare URL inside custom markup.
  Both tags are held back from the normal merge pass and resolved against the
  already-assembled body, so they survive the intro block's HTML escaping.
  Nothing is appended automatically — a send without the tag is unchanged.
- Files affected: `CLAUDE.md`, `api/_brevo-email.js`, `api/_proto-urls.js`,
  `src/components/CustomerEmailModal.jsx`, `src/lib/emailMergeTags.js`, and
  `src/lib/protoUrls.js`.
- Systems affected: Admin email composer and the Brevo broadcast send path
  only. No database migration and no change to Brevo's automatic
  List-Unsubscribe header or suppression list.
- Verification: Email suites pass (39/39); lint and production build pass. The
  full suite is 683/684 with 6 failing files, all Image Processing Centre / fal
  provider, reproduced unchanged on `origin/main` at `1931021`. The
  `qa-smoke-check.mjs` specific-people-audience assertion also fails identically
  on `origin/main`; both predate this work. Post-deploy, the live bundle
  `assets/CustomerEmailModal-DHVTJH7p.js` on https://admin.proto.co.za was
  confirmed to contain the Unsubscribe chip and both tags.
- Deployment status: Deployed to production. `dpl_2UaGDgciGVYTZ9f8DGXzyrseBJ2s`
  (`protoportal-admin-esaqq9yiq-proto-team.vercel.app`), READY at 00:40 SAST,
  built from `main` by the GitHub integration.
- Commit or PR: `1ca7a38` (reapply) and `1958aa8` (journal), fast-forwarded onto
  `main`. No pull request — `gh pr create` and the GitHub MCP equivalent were
  both blocked by the local permission classifier, so the branch
  `restore-unsubscribe-tag` was merged locally and pushed.

## 00:40 SAST — Production returned to a `main`-built deployment

- Summary: Recorded because it changes deployment behaviour, not code. Before
  this deploy, production was serving `dpl_AR55P3QjRAUokK58Dq3TMVuWv5uG`
  (2026-08-24 21:57 SAST), pushed from the Vercel CLI off an unpushed and dirty
  `fix/featured-order-merge` working tree. Its commits — `49cc36bf` ("Add bulk
  product flag actions"), `b351add0`, `764728d2`, `ba987712` — exist on no
  branch of this remote, in no pull request, and nowhere on `main` under a
  squashed SHA. Deploying `main` therefore dropped that work from production.
  Daniel was shown this before the deploy and chose to proceed with rollback as
  the safety net.
- Systems affected: Vercel project `protoportal-admin` production alias only.
- Verification: Confirmed via `git ls-remote`, an all-refs message search, and
  the open pull request list that the four commits are not recoverable from Git.
- Deployment status: Superseded deployment remains a rollback candidate in
  Vercel. Restoring it would also remove the Unsubscribe chip again, since the
  two states have no common commit.
- Commit or PR: N/A — deployment-state record. The bulk product flag and bulk
  featured reorder work needs to be pushed from its author's machine before it
  can return to production.

## 11:22 SAST — Fix group email sends rejected with "Choose a group to send to."

- Summary: `sendCustomerEmailBroadcast` in `src/lib/customers.js` destructured
  every field of the compose payload except `groupId`, so the group chosen in
  the composer never reached `api/customer-email-broadcast.js`. That endpoint
  rejects `audience: 'group'` without a group id, and because the check runs
  ahead of the test-send branch it blocked Send test as well. Every send to a
  group has failed this way since Groups shipped in `0c5c57e` — the error reads
  like an unset audience picker, which is why it looked like a UI fault.
- Files affected: `src/lib/customers.js` and the new
  `tests/email-broadcast-group-payload.test.js`.
- Systems affected: Admin email composer group sends only. No API, schema or
  Brevo change — the endpoint was already correct.
- Verification: New regression test asserts on the request body and was
  confirmed to fail against the unpatched client (2 of 4 cases) and pass after.
  Email suites 43/43; lint and production build pass. The pre-existing Image
  Processing Centre / fal failures are unchanged and unrelated.
- Deployment status: Pending push at the time of writing.
- Commit or PR: `21dce67`. No pull request — pushing to `main` deploys.
