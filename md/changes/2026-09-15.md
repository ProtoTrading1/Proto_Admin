# 2026-09-15 — Apollo activity and memory reporting correction

- Replaced the Pulse's old `customer_visits` heartbeat-duration estimate with the existing coverage-aware reader over the dedicated Apollo activity database. The service-role client is server-only, owner-gated and has no portal-database fallback.
- Added `APOLLO_ACTIVITY_REPORTING_ENABLED`, default off. Missing/disabled collection remains unavailable. If collection health does not prove continuous coverage across Main and Instore, the response is `partial`; partial intervals are shown only as diagnostics and do not feed an engagement total or Apollo insight.
- Corrected the active-interval query to exclude completed-search, product-view, category-view and basket-add events instead of treating those valid event types as malformed intervals. Added a source-separated Main/Instore search funnel reader, retaining original wording, normalized terms, zero-result counts, product/category views and follow-on basket-add events. Same-session attribution is explicitly labelled association rather than causation.
- Connected the owner-only Pulse panel to the paginated approved-memory API, including a working next-page control. Entries include reviewer identity, version and evidence references; current business facts remain in live report sources, not memory.
- Made the live-customer panel explicitly say that current section is not collected yet rather than silently rendering a null value.
- Removed authenticated customer IDs from the summarized activity response; the owner view receives aggregate active-customer counts only. Incomplete intervals return a null period total.
- Updated Pulse UI, API, activity/search readers, memory contract and insight tests. Targeted Vitest: 52 tests passed across seven files. `npm run lint` and `npm run build` passed.
- Release gates remain open for signed-in isolated-preview journeys, coverage/watermark verification, Positill field and sales reconciliation, privacy/access checks and browser testing. No feature flag, hosted environment, migration or production deployment was changed.
- Current-state check: Supabase still reports the Apollo test branch `MIGRATIONS_FAILED`; its four same-named ledger entries use versions different from local migration filenames, and the branch omits `customer_visits` present on the parent. Apollo objects/RPCs exist and RLS/service-only grants were verified, but Data API exposure and preview-secret endpoint access remain unverified. Branch not reset/rebased; no database writes.
- Wider regression run: all 52 focused Apollo admin tests, lint and build passed. Full admin suite had 912 passing tests and two out-of-scope failures (email-campaign follow-up copy contract and Instore image-control action-string contract). The storefront's 27 focused Apollo activity tests, changed-file ESLint and production build passed; full storefront suite/lint still fail on out-of-scope customer-journey contract assertions and three unused-symbol lint errors. No unrelated source files changed.

### Fresh release-readiness verification

- Re-read the current branch/repository state on 2026-09-15. The Apollo admin preview worktree is divergent from its remote (`ahead 1, behind 1`) and has uncommitted changes; the storefront Apollo worktree is also uncommitted. Neither current local tree is represented by a verified deployed preview. No push or deployment was made.
- Fresh Supabase read-only inventory: production portal project is `ACTIVE_HEALTHY`; public tables include 7,870 products, 90 website orders, 168,576 existing analytics events, 11,048 search records, 1,446 saved baskets, 1,199 current-presence rows, and 3,041 visit sessions. The Apollo branch remains `MIGRATIONS_FAILED`, `with_data: false`, and exposes only nine public tables including the Apollo foundation. It is unsuitable for end-to-end sales/activity validation; row counts are a dated inventory, not a metric claim.
- The local Apollo test suite passes 109/109; admin production Vite build and changed-file lint pass. Full admin suite: 921/923 pass, with the existing email-campaign label assertion and Instore image-control action-string assertion failing outside Apollo.
- Corrected the storefront `npm test` script to use Node's native discovery rather than a quoted glob that silently ran zero tests on Windows. Native discovery runs 384 tests: 383 pass and the existing `customer-journey-lifecycle` static contract assertion fails. Storefront production Vite build and changed-file lint pass; full lint still has three unused-symbol errors in existing `api/extended-range.js` and `src/components/ExtendedRangePage.jsx`.
- Tried the in-app browser automation to retrieve the Supabase branch failure log; it could not initialize (`failed to write kernel assets`). No dashboard or database mutation occurred. The exact initial failed statement is still required before any migration-ledger repair, reset, rebase or new Apollo DDL.
- Deployment remains blocked. Release gates for real authenticated source-separated collection, source-health watermarks, data reconciliation, browser verification, and rollback baseline remain open. No production settings or data were changed.

### Coarse live-presence section capture

- Apollo’s owner-only live view now reads the nullable coarse `customer_presence.current_section` when available, labels it for owners, and falls back to “not collected yet” on older portal schemas. The branch remains unready: new schema migration failure still must be diagnosed before any preview DB change.
- Verification: 35 targeted tests across five Apollo suites passed; changed-file ESLint passed; admin production build passed and `vercel.json` parsed. No deployment, environment, Supabase, or production changes made.
- Preview evidence refreshed: existing Apollo preview `dpl_9uSpCcEc8s9Jz2WcPUD5bSFrFNCR` is READY at commit `ac54b94a58d20f6f1c5e8f770baa331bff46a17a`, before current uncommitted changes. Protected preview fetch still cannot be authorized here. Production alias is `admin.proto.co.za` on READY deployment `dpl_3PhNAoQGXsn6ZgqUXcPns5y2U8k1`, a promotion with no Git SHA; it remains the rollback target and current baseline ambiguity.

### Retention schedule and Vercel cron authorization

- Added a daily 03:30 SAST retention invocation (01:30 UTC) to the Vercel project config. The route accepts Vercel's GET only with a matching `CRON_SECRET`/dedicated secret; owner-authenticated POST remains supported. Retention stays off by default and only calls the fixed database RPC.
- Verification: the 8-case retention API suite (including Vercel GET/bearer handling, default `CRON_SECRET`, and the single daily UTC schedule) passed within the 35-test Apollo run; changed-file ESLint and admin build passed. Config is local only; no Vercel deployment or environment change made.
### Apollo release audit continuation

- Rechecked live Supabase branch metadata read-only: `apollo-business-pulse-20260912` remains `MIGRATIONS_FAILED`, its preview project is `ACTIVE_HEALTHY`, and its branch ledger has four Apollo migrations whose version numbers do not match the four local SQL filenames. Captured exact versions in `md/apollo-release-gates.md`; did not reset, rebase, or apply migrations.
- Attempted a read-only Supabase project-health lookup via the connected Management API integration; the account returned HTTP 403. Stopped there and did not try alternate credentials or routes. The exact first migration failure remains unknown and must come from the dashboard's branch View Logs.
- Re-audited the existing Positill path: Apollo only calls the established read-only `top-sellers` bridge for the current SAST day, and labels returned DBINVDT totals as incomplete raw evidence with VAT, credit-note and website-reference semantics explicitly unknown. Week/month/custom POS sales and reconciliation remain unavailable; no new SQL or bridge route was added.
- An attempted rerun of the Apollo-focused Vitest suite could not start because this checkout's `node_modules` junction points to a sibling workspace whose directory access is denied by the current environment. Earlier focused test/build results remain the last successful verification; this attempt is not counted as a pass.
- No Supabase schema, Vercel settings, Git remote, deployment, production behavior, or customer data changed.
- Follow-up read-only branch audit confirmed all Apollo tables have RLS enabled and grant no table privileges to `anon` or `authenticated`; service-role table access is present. Apollo append/read/retention functions grant no EXECUTE to public client roles and their service-role grants are limited to the three server RPCs. The preview `customer_presence` schema still lacks `current_section`. Exact checks are recorded in the release-gates document.
- Continued local reliability work found and fixed an order-value discrepancy: Apollo now subtracts the persisted VAT-inclusive `discount_amount`, reports the applied discount total, and fails closed for invalid negative/malformed money or unknown order statuses. Focused regression coverage verifies that unknown statuses are not silently treated as pending.
- Corrected open day/week/month comparisons to use matched elapsed-to-date windows rather than a partial current period against a full prior period. The preview marks the current window as period-to-date and the UI explains the comparison basis. Custom ranges retain equal-duration previous-period comparisons.
- Verification after these changes: all 113 Apollo-focused admin tests pass; admin Vite production build passes with 2,210 modules; changed JavaScript files pass ESLint. These changes are still uncommitted/local and are not in the READY preview or production.
- Final current-tree checks: full admin ESLint passes; the full admin Vitest suite is 926/927 (one unrelated existing email follow-up contract failure remains); all 113 Apollo-focused tests pass; production Vite build passes. A stale Instore image-control test was corrected to assert the actual panel `change('restore')` action and its focused four-case suite now passes. No admin/email product behavior was changed to silence the unrelated failure.

### Prevent insights from incomplete evidence

- Apollo decision insights now require an available, complete source envelope and complete payload; website-sales insights additionally require an explicitly known revenue value. This prevents partial event feeds or unknown order amounts from being phrased as confident decisions.
- The Pulse empty-priority state now says no *verified* decision insight is available and points owners to source completeness, instead of implying there was nothing recorded.
- Verification: Node syntax check passed; three direct executable scenarios passed (complete sources still produce insights, partial/unavailable sources do not, unknown revenue does not produce a sales insight); `git diff --check` passed. Vitest/UI suite remains unverified in this environment due to the denied `node_modules` junction.
- Scope is local Apollo preview work only; nothing was pushed, deployed, or enabled.
- Full Apollo-focused admin Vitest verification subsequently ran with Vitest's `runner` config loader to avoid the junction bundling failure: **106/106 tests passed across 13 files**. A direct Vite production build also passed (2,210 modules). Targeted ESLint had no errors; it reported the JSX UI test as ignored because the current ESLint config supplies no matching rule set for that test file.
- Full admin regression suite also ran: **918/920 passed across 108 files**. The two failures are pre-existing/out-of-scope contract checks in email follow-up wording and Instore image-control restore source text; all 106 Apollo tests pass in that full run. `git diff --check` passes.
- A fresh read-only Vercel listing confirmed the latest Apollo branch preview remains READY at `protoportal-admin-ax7l6jisn-proto-team.vercel.app`, commit `ac54b94a58d20f6f1c5e8f770baa331bff46a17a`—older than the local uncommitted work. Production still points at promoted deployment `dpl_3PhNAoQGXsn6ZgqUXcPns5y2U8k1` without a Git SHA; no alias promotion occurred.

### 21:00 SAST — Correct calendar day/week comparison windows

- Read-only code review found that open day/week comparisons were equal-duration windows immediately before the current window, not the matching elapsed portion of the prior calendar day/week. Changed day comparison to yesterday at the same elapsed point and week comparison to the prior Monday-start week at the same elapsed point. Month and custom-range behavior is unchanged.
- Updated boundary expectations in `tests/apollo-reporting.test.js`. Apollo report labels already distinguish matching-period-to-date comparisons.
- The first rerun exposed two API-test fixtures still tied to the previous equal-duration range. Updated those fixtures to assert the correct prior-calendar-day window. The focused Apollo suite now passes **114/114** tests.
- Hardened the Positill evidence adapter: the bridge's current `today` rollup is now unavailable for historic days, custom ranges, or any day-to-date window that is not the current SAST day. Added a regression for stale-day requests.
- Renamed the Pulse section to “Positill report evidence (not verified sales)” and added a UI assertion, so unverified aggregates cannot be read as confirmed sales from the heading alone.
- Admin ESLint and Vite production build pass. The full admin suite is **927/928**; the sole failure remains the unrelated email follow-up label contract test. Current local source is not deployed.
- Revalidated external state at 21:02 SAST: Apollo Supabase branch still `MIGRATIONS_FAILED`; admin preview remains at older commit `ac54b94a…`; production `admin.proto.co.za` remains on promotion `dpl_3PhNAoQGXsn6ZgqUXcPns5y2U8k1`. No push, deployment, migration, environment change, or production mutation.
- Follow-up audit caught and fixed an engagement-insight schema mismatch: decision text now consumes `activeCustomers`/`activeSeconds` from the real activity reader and fails closed for stale or invalid fields. The Pulse Positill heading explicitly says “not verified sales.” Focused Apollo tests pass **115/115**, admin lint/build pass.
- Fixed Instore category-interest capture to record the active surface rather than Main-only. Storefront regression suite passes **383/383** and production build passes. These remain local; current preview and production deployments are unchanged.
- Added a constrained “Ask Apollo” layer over the already owner-gated, server-computed pulse feeds. It supports live customers, baskets, recorded searches, deliberate product/category views, website orders/comparisons, active-time availability and Positill status; it shows source, period and last read, rejects unsupported/forecast questions, and labels partial evidence. Questions stay in page memory and are not sent to an external model or persisted. This is a first parameterized report interface, not free-form AI Q&A or a verified release.
- Apollo-focused suite now passes **125/125** across 14 files; the admin lint and build passed immediately before the final one-line source fallback/test addition and must be rerun before preview. No publication or environment/database changes.

