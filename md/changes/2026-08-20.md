# Changes — 2026-08-20

## 15:15 SAST — Local repository and change-journal setup

- Summary: Cloned the Proto admin portal into `/Users/danieljoffe/Desktop/Proto`
  and established a mandatory dated change-journal rule for future work.
- Files affected: `CLAUDE.md`, `AGENTS.md`, `md/README.md`, and this journal file.
- Systems affected: Local development workspace only.
- Verification: Confirmed the clone tracks `origin/main` and reviewed the repository
  structure, scripts, authentication boundary, serverless APIs, Vercel configuration,
  and existing project instructions.
- Deployment status: Not deployed; no application behaviour changed.
- Commit or PR: Not committed.

## 15:28 SAST — Featured product live-order grid

- Summary: Rebuilt Site Content → Featured ordering as a responsive numbered
  merchandising grid. Products can be reordered with mouse or touch dragging,
  arrow keys, or explicit earlier/later controls. Added drag cancellation,
  visible drop targeting, save-state feedback, responsive layouts, and protection
  against focus refresh overwriting queued edits.
- Files affected: `src/components/FeaturedPanel.jsx`, `src/index.css`, and
  `tests/featured-grid.test.jsx`.
- Systems affected: Admin Featured editor only. The stored
  `site-config/featured-products.json` SKU sequence and customer-portal reader
  contract are unchanged, so saved grid order remains the live portal order.
- Verification: Feature ordering and live-sequence persistence tests 4/4 passed;
  admin routing tests 2/2 passed;
  ESLint passed; production Vite build passed. Repository-wide Vitest run passed
  725/730 tests. Four image conversion timeout failures passed when isolated with
  a suitable timeout; one unrelated pre-existing image queue refresh regression
  still fails. The existing QA smoke script also stops on a stale Email CRM
  audience assertion unrelated to this feature.
- Deployment status: Deployed to `admin.proto.co.za`; Vercel GitHub check passed
  and the production alias served the new Featured grid bundle.
- Commit or PR: `621c49f`.
