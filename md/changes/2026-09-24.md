# 2026-09-24

## Stock database: stored Instore read model (migration 070)

**Time:** 14:20 SAST

**Summary.** Added `migrations/070_instore_catalogue.sql` and applied it to the
stock Supabase project (`yiqsvwajozafvalwcero`). Note the number is shared with
`070_whatsapp_crm.sql`, written in parallel today: the two target different
databases (stock vs website) and duplicate numbers already exist in this
directory (057, 058, 059, 061), so both files keep their numbers. It creates the tables the main
portal now uses to serve Instore Products from an already-gated snapshot instead
of rebuilding the collection from `extended_range_items` and `website_stock` on
every customer request. No admin code changed and no existing table, function,
product, price, stock figure or Instore control was modified.

**What was created.**

- `public.instore_catalogue` — one row per eligible Instore product: its exact
  customer response payload, its browse category, its search tokens and its
  position in the default customer ordering.
- `public.instore_catalogue_staging` — a refresh stages its rows here and the
  swap happens in one statement, so a customer can never see a half-written
  collection.
- `public.instore_catalogue_state` — a single row holding the snapshot's
  freshness, product count, browse tiles, a fingerprint of the Instore
  image/listing controls it was built under, and the refresh lease.
- Functions `instore_catalogue_claim_refresh`, `instore_catalogue_release_refresh`,
  `instore_catalogue_stage`, `instore_catalogue_commit`.

All three tables have RLS enabled, are revoked from `anon` and `authenticated`,
and are granted only to `service_role`; the four functions are likewise
service-role-only. This is a cache of an eligibility decision, never a source of
truth — the portal ignores it when it is missing, stale, or built under
different Instore controls than the ones currently in force, and checkout still
verifies every line against the live index and the stock bridge.

**Effect on the admin app.** None required. `api/instore-image-controls.js` is
unchanged: the portal reads `instore_image_controls` and
`instore_listing_controls` live on every request, so hiding an Instore image or
listing from Product Manager still takes effect immediately.

**Verification.**

- Objects confirmed present: 3 tables, 4 functions, 1 state row.
- Functions exercised directly against the stock project: the refresh lease
  admits one claimer and refuses a second while held, refuses a claim while the
  snapshot is fresh, a staging count mismatch is rejected, a blank-SKU row is
  skipped, the commit swaps rows, stores tiles and clears the lease, and the
  stored-token prefix match (`LIKE '% wood%'`) works.
- Test rows were deleted and the state row reset; `instore_catalogue` and
  `instore_catalogue_staging` are empty pending the portal's first refresh.
- Expected first snapshot size, measured in SQL: 4,164 products (4,172 eligible,
  less 5 already on the main site and 3 hidden listings).

**Deployment status.** Migration applied to the production stock database.
Migration file committed and pushed to `claude/youthful-bohr-dhob72`. The
consuming code lives in ProtoMainSite (see that repository's journal entry for
today); no admin deployment is needed.

## Stock database: fix the Instore snapshot swap (migration 071)

**Time:** 17:25 SAST

**Summary.** Migration 070's snapshot swap could never run through the portal.
The Supabase REST role (`authenticator`) preloads `safeupdate`, which rejects an
UPDATE or DELETE with no WHERE clause — including statements inside a function —
so `delete from public.instore_catalogue;` failed with "DELETE requires a WHERE
clause" every time `instore_catalogue_commit` was called through the API.

`migrations/071_instore_catalogue_commit_safeupdate.sql` recreates the function
with `delete from public.instore_catalogue where sku is not null`. `sku` is the
primary key and therefore never null, so it matches every row and satisfies the
guard. Nothing else changes: the swap is still one transaction, an incomplete or
empty batch is still rejected, and an unchanged collection is still revalidated
without rewriting its rows. Applied to the stock project.

**How it was missed.** Migration 070 was verified by calling the functions
through this Supabase connection, which authenticates as `postgres` and has no
`safeupdate` preload, so the commit succeeded there. The guard only applies to
writes arriving through the REST API, which is how the portal calls it. Any
future migration that adds an unfiltered UPDATE or DELETE inside a function
needs checking against the REST path, not only a direct connection.

**Impact while it was broken.** No customer saw a wrong product and no data was
lost. The portal handles a failed snapshot write by logging it and releasing the
refresh lease, so it kept serving the correct products from its live read — it
simply never got any faster, because the snapshot could not be published. The
failure was recorded in `instore_catalogue_state.last_error`, which is how it
was found.

**Verification.**

- The replacement path — the one that failed, where a changed collection
  deletes the previous rows before inserting the new ones — was run end to end
  after the fix: two products committed, then a different single product
  committed over them, leaving only the new row.
- Every write statement in all four `instore_catalogue*` functions was listed
  from `pg_proc.prosrc` and confirmed to carry a WHERE clause: the snapshot
  delete by `sku is not null`, the staging cleanup by batch and age, and both
  state updates by the singleton `id`.
- The guard itself could not be loaded into this session (`access to library
  "safeupdate" is not allowed` for this role), so the fix is verified
  structurally plus functionally, not by reproducing the rejection here.
- Test rows removed and the state row reset: `instore_catalogue` and
  `instore_catalogue_staging` are empty, `last_error` is null, and the snapshot
  is unbuilt pending the portal's next request.

**Deployment status.** Applied to the production stock database. Because it is a
database function, it takes effect for every already-running deployment with no
redeploy.

## 20:55 — WhatsApp CRM tab (WATI broadcasts, analytics, opt-outs)

Added a `whatsapp` admin section: a WhatsApp CRM for the customers who opted in
to WhatsApp, sending template broadcasts through the Proto WATI account, with
delivery/read/click analytics and an enforced opt-out list.

**Scope decision recorded.** `CLAUDE.md` previously banned all outgoing
WhatsApp to customers. The owner explicitly asked for this capability, so the
ban has been narrowed rather than ignored: consented template marketing is now
supported; WhatsApp order alerts, welcome messages and the Intercom relay stay
removed. `CLAUDE.md` records both the reinstatement and what remains banned.

**Consent rule** (the core of the change) — three conditions, all required, and
re-resolved server-side on every send including a hand-picked list:
`customers.accept_whatsapp = true`, a number that normalizes to a valid one, and
no row in `whatsapp_opt_outs`. `api/_whatsapp-audience.js` is the single gate;
a phone list from the browser filters the eligible set and can never widen it,
so a stale tab cannot reach someone who opted out minutes earlier. The same
resolver backs the Contacts screen and the dashboard, so the audience count
shown is the count that gets messaged, with a per-reason breakdown of who is
held back.

**Files / systems affected**

- Migration `migrations/070_whatsapp_crm.sql` — additive. Extends the
  `whatsapp_contacts` / `whatsapp_messages` / `whatsapp_broadcasts` /
  `whatsapp_webhook_events` tables that survived migration 055 (055 dropped only
  `whatsapp_sessions`), adds `whatsapp_opt_outs`, and adds the
  `whatsapp_register_click(text)` RPC. Nothing was dropped, so the 96 existing
  `whatsapp_contacts` rows were preserved.
- Backend helpers: `api/_wati-client.js` (WATI HTTP client — deliberately
  separate from `_wati-notify.js`, the internal fulfilment-team alert, so the two
  blast radii stay distinct), `api/_whatsapp-audience.js`,
  `api/_whatsapp-broadcast.js`, `api/_whatsapp-phone.js`.
- Routes: `whatsapp-dashboard.js`, `whatsapp-contacts.js`,
  `whatsapp-templates.js`, `whatsapp-broadcast.js` (send + history + per-send
  funnel), `whatsapp-sync.js` (cron), `whatsapp-opt-outs.js`, `wati-webhook.js`,
  `wa-click.js`.
- UI: `src/components/WhatsappPanel.jsx` and `src/components/whatsapp/`
  (Dashboard, Contacts, Composer, Analytics, Opt-outs), `src/lib/whatsappFormat.js`.
  Wired into `GroupedSidebar.jsx` and `AdminPage.jsx`; the section is available to
  the customer-service role, but lifting an opt-out is owner-only.
- `vercel.json`: `/api/whatsapp-sync` cron every 3 hours; `maxDuration` for the
  broadcast, sync, contacts and dashboard routes.
- Test `tests/whatsapp-crm.test.js` (26 tests). Updated the
  `CUSTOMER_SERVICE_SECTIONS` assertion in `tests/product-intelligence.test.js`
  to assert the role invariant instead of a frozen array.

**Design notes worth keeping**

- Broadcasts are always WATI templates WhatsApp has marked `APPROVED`. Outside a
  24-hour customer-initiated window WhatsApp silently drops non-template
  messages, so a free-text composer would report a successful send of ~1 500
  messages that nobody received.
- WhatsApp has no link-click webhook. A broadcast whose parameter contains
  `{{link}}` gets a per-recipient tracked link through `api/wa-click.js`, which
  is how "who clicked" is answered. The redirect is public by design (the clicker
  has no admin session) and is not an open redirect — the destination comes from
  the broadcast row, not the request, and is https-guarded.
- Phone normalization was written against the real column, which holds
  `0821234567`, `27821234567`, `270821234567`, `821234567`, `0027821234567`,
  Cape Town landlines, and fields containing two numbers. Ambiguous values are
  reported to the admin as unusable rather than guessed at and messaged.
- Delivery webhooks arrive out of order, so status only ever moves forward
  (queued → sent → delivered → read) and the analytics funnel is recomputed from
  the recipient rows rather than read off the broadcast's counters.
- Opt-outs are captured from a STOP-style reply, from a customer switching
  broadcasts off inside WhatsApp (read back by the sync's pull half), and from an
  admin. The first opt-out keeps its original timestamp — a later duplicate reply
  must not overwrite a compliance answer.

**Verification**

- `npx eslint src lib api tests` — clean.
- `npm run build` — passed; the five WhatsApp chunks are emitted.
- `npx vitest run` — 847 passed, 26 of them the new WhatsApp tests. Two failures
  remain (`instore-image-controls-contract`, `email-campaign-follow-up`); both
  fail identically on `main` and are unrelated to this change.
- Two defects were caught by the new tests and fixed before commit: the opt-out
  matcher missed the polite form ("Please stop sending me specials"), and the
  module-boundary test was matching a comment rather than an import.
- The WATI API itself could **not** be exercised: `live-mt-server.wati.io` is
  blocked by this session's egress policy, so template loading, contact push and
  the first live send are untested against the real service and must be verified
  in production once the token is set.

**Database**

- Applied to the Proto Trading Website Supabase project (`kyodrsqnmihwoplkhwwf`)
  in two migrations, `whatsapp_crm` and `whatsapp_register_click`. Verified the
  five tables and their new columns exist. New tables are RLS-enabled with
  `anon` / `authenticated` revoked and `service_role` granted; the click RPC is
  service-role-only.

**Deployment**

- Not deployed. Pushed to `claude/affectionate-noether-3t2u3e`.
- **Required before the tab can send anything** — set in Vercel:
  `WATI_API_TOKEN`, `WATI_API_URL`, and `WHATSAPP_WEBHOOK_SECRET` (configure WATI
  to send the same value as an `X-Webhook-Secret` header on its webhook, pointed
  at `/api/wati-webhook`). Optional: `WHATSAPP_CLICK_BASE_URL`,
  `WHATSAPP_CLICK_FALLBACK_URL`. No secrets are recorded in this repository.

## 21:12 — WhatsApp connection status indicator

- Added a connection status button to the WhatsApp CRM header: green when a real
  authenticated call to WATI just succeeded, amber when WATI is reachable but no
  webhook events have ever arrived, red when the token is rejected, grey when no
  token is set. Clicking it shows the WATI account id, approved-template count,
  webhook secret state, last event received and 7-day event count, and forces a
  re-check.
- The green state deliberately requires a live WATI call rather than the presence
  of `WATI_API_TOKEN`: an expired token satisfies the env var and fails only at
  send time. "WATI reachable" and "webhook delivering" are reported separately
  because they fail independently — sending can work perfectly while a mistyped
  `X-Webhook-Secret` leaves every broadcast reading 0 delivered.
- Files: `api/whatsapp-status.js`, `src/components/whatsapp/WhatsappConnectionStatus.jsx`,
  `src/components/WhatsappPanel.jsx`, `vercel.json` (30s maxDuration),
  `tests/whatsapp-crm.test.js` (+2 tests, 28 total).
- Verification: `npx eslint src lib api tests` clean; `npm run build` passed;
  `npx vitest run` 850 passed. The same two failures as earlier
  (`instore-image-controls-contract`, `email-campaign-follow-up`) still fail
  identically on `main` and are unrelated. The WATI call itself remains untested
  from this environment — `live-mt-server.wati.io` is blocked by the session's
  egress policy, so the indicator's green path is first proven in production.
- Deployment: merged to `main` and pushed; Vercel rebuilds `protoportal-admin`.

## 21:12 — Vercel environment variables (WhatsApp CRM)

- `WATI_API_TOKEN`, `WATI_API_URL` and `WHATSAPP_WEBHOOK_SECRET` added to the
  `protoportal-admin` project, Production scope, all as Secret type. Values are
  not recorded here.
- A redeploy is required for the running deployment to pick them up.
