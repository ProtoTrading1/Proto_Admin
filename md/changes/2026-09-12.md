# 2026-09-12

## Instore listing removal control prepared

- Added a separate, reversible owner-only Instore listing control. It is deliberately distinct from photo hiding: an image hide preserves sale eligibility, while a listing hide removes the exact SKU from Instore discovery and checkout until restored.
- The pending migration `069_instore_listing_controls.sql` adds private, RLS-protected control and audit tables plus a service-role-only `SECURITY INVOKER` RPC. It does not update product, stock, price, image, or main-catalogue records.
- Applied to the Proto Stock Tracking Supabase database after explicit authorization. Verification found zero control records; anon cannot read or execute the RPC, while service_role can.

## 09:25 — Instore image-only safety controls (preview branch)

- Added an owner-only Image Processing Centre workflow that hides or restores an incorrect Instore product photo by exact SKU.
- Added the pending stock-database migration `068_instore_image_controls.sql`: private control and event tables plus a service-role-only, atomic audit RPC. It does not modify product records, prices, stock, catalogue classification, or ordering eligibility.
- Affected systems: admin API and owner UI; the customer storefront branch reads the new control state and presents an “Image being updated” placeholder while retaining checkout eligibility.
- Verification: admin lint and production build passed. The focused Vitest command is blocked locally by the worktree's esbuild config-loader permission error; the test source is committed for CI.
- Deployment: application changes not deployed; the database migration is recorded below; commit `3d91c28` was amended to record this journal update.

## 09:42 — Applied private Instore image-control schema

- Applied migration `068_instore_image_controls.sql` to the Proto Stock Tracking Supabase database (`yiqsvwajozafvalwcero`).
- Verified both new tables exist; `anon` cannot read the control table or execute the update function, while `service_role` can. No control records, product records, prices, stock values, or images were changed.
- Deployment: application branches remain preview-only; commit reference pending this journal update.

## 22:58 SAST — Apollo Business Pulse first implementation increment

- User approved the Apollo reporting/customer-activity plan and requested lower-cost parallel agents. This is a narrow user-authorised exception to the historical removed-Apollo rule; no old Apollo application was restored.
- Verified current admin domain deployment dpl_AoshhRH8yeLn13YqDwMjaJiJQtMb; all 619 non-generated deployed source hashes matched the admin-instore-image-controls baseline before copying a clean candidate. Storefront target dpl_E2XrLK9zfz2RHPLQXMzVKgcewnaM: only generated npm-cache differences, excluded from recovery.
- Added lib/apollo-reporting.mjs, api/apollo-pulse.js, src/components/ApolloBusinessPulse.jsx and focused tests. Added an optional AnalyticsHub tab, disabled unless VITE_APOLLO_PULSE_ENABLED=true; API separately requires APOLLO_PULSE_ENABLED=true and a verified owner session. No new top-level navigation, old engine, pricing, checkout or order mutations.
- Reporting uses SAST Monday-start weeks, explicit VAT-inclusive website order values, bounded paginated reads, unknown-source states, strict POS semantics and explicit-reference-only reconciliation helpers. POS sales and memory are deliberately unavailable pending verified integration. Presence joins only current customers to recorded cart snapshots. UI handles evidence envelopes, stale results, filter isolation and expired owner access.
- Read-only backend verification: admin portal URL is kyodrsqnmihwoplkhwwf; stock URL is yiqsvwajozafvalwcero. Production portal has no public.apollo_memories. Existing preview branches both report MIGRATIONS_FAILED.
- With explicit user cost approval (provider quoted 0.01344 hourly), created Supabase branch apollo-business-pulse-20260912, project_ref ppnlkkvdouxsftscdpei, parent kyodrsqnmihwoplkhwwf, without production data. Initial state CREATING_PROJECT. No live migration, production configuration, catalogue or customer data change.
- Verification: 12 reporting tests + 7 API tests + 4 UI tests passed; admin npm run lint and npm run build passed. Build mutation presteps found no pending work and skipped. Separate storefront tracking increment passes 30 focused tests and build; its baseline has three unrelated lint errors.
- Not complete: active-time collection, source-tagged Instore search capture, retention/rollups, invoice credit/VAT verification, production-safe reconciliation, memory schema/review history, Apollo natural-language tools, isolated integration tests and release verification. No deployment or commit yet. Production release remains gated.

### 23:00 SAST — Preview schema bootstrap blocker

- New Apollo branch ppnlkkvdouxsftscdpei is ACTIVE_HEALTHY but reports MIGRATIONS_FAILED; information_schema confirms no public application tables. Production ledger begins with migration 20260611192400 order_workflow_status, which ALTERs public.orders before any recorded table-creation baseline. No production repair attempted.
- Recovery comparison passed: no deleted baseline files and no modifications outside the allowed-change manifest. Existing change-journal entries preserved.
- Following independent review, tightened Monday week boundaries, malformed calendar dates, null/blank amounts, capped/invalid pages, cancelled-order rankings, nested product snapshots, and explicit POS sign conventions. UI regression tests exercise live response envelope, unavailable monetary data, refresh cadence, stale data/filter isolation and owner expiry.

### 23:04 SAST — Synthetic database fixture verification

- Created only five minimal read-model fixture tables in the explicitly authorised Apollo test project ppnlkkvdouxsftscdpei, using ../tests/preview-fixture.sql. This is NOT a production migration, complete schema baseline, or repair to the parent's migration ledger. Only synthetic products/customers/orders/searches were inserted.
- Verified fixture SQL returns 2 orders and R115 non-cancelled VAT-inclusive order value. Verified RLS enabled on all five tables, SELECT denied to anon/authenticated and allowed to service_role. No real customer records copied.
- Full candidate Vitest run: 833 passed, 2 failed. Both failing assertions reproduce on the unchanged production-source checkout (email-campaign-follow-up and instore-image-controls-contract). Focused Apollo tests all pass. Full-suite release gate therefore remains open; unrelated tests were not modified to manufacture a pass.
- Rebuilt with VITE_APOLLO_PULSE_ENABLED=true successfully as well as with the default disabled flag. Production remains unchanged and no hosted Apollo preview is deployed.

### 23:23 SAST — Multi-agent activity and memory foundation

- Rechecked production deployment identities: admin dpl_AoshhRH8yeLn13YqDwMjaJiJQtMb and storefront dpl_E2XrLK9zfz2RHPLQXMzVKgcewnaM unchanged. Continued only isolated candidates with two lower-cost implementation agents and one independent reviewer. Parent corrected and tested incomplete agent drafts before accepting.
- Corrected SKU-vs-internal-ID product ranking, missing-item completeness and malformed-search handling. Added as-of/period-closed evidence. Added tested per-source coverage and overlapping-tab active-interval union. Empty unverified sources remain unknown.
- Added strict approved-memory definitions/decisions validation, full revision-history consistency, reviewer-context validation, fail-closed current retrieval and bounded version checks. Newer unapproved/rejected revisions suppress older approved text. No natural-language memory route enabled yet.
- Applied three additive migrations ONLY to explicitly approved Apollo branch ppnlkkvdouxsftscdpei: apollo_activity_foundation, apollo_activity_retention, apollo_memory_revisions. Generated source filenames using pinned Supabase CLI migration new. Added private events, explicit collection health, non-identifying monthly counts, append-only memory revisions and service-only version-checked RPC. No live project schema/data/env changes.
- SQL integration tests ../tests/activity-retention.sql and ../tests/memory-revisions.sql passed in rollback transactions: retention repeat-run/counts, expired monthly removal, memory conflict/evidence/immutability/different-reviewer history, browser-role privilege denial and service-role execution. Synthetic test rows were rolled back. Retention scheduler is NOT enabled. Monthly summaries retain event counts only, not free-text searches or historical active-duration totals.
- Supabase security advisor: only informational RLS-with-no-policy findings, intentional service-only tables with browser grants revoked. Reference: https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy . No warning/error advisor findings returned.
- Verification: all 48 focused admin Apollo tests passed; full suite 858 passed, same 2 baseline assertion failures. Admin lint/build passed including enabled-feature build. Storefront new queue/activity tests 11 passed; storefront build and focused changed-file lint passed, full lint retains 3 baseline errors. No production deployment/commit/PR.
- CLI created supabase/.temp/cli-latest; removed only this generated version-cache file to keep recovery candidate allowlist clean. Dependency CLI cache remains outside candidate.
- Remaining: strict server event ingest/DB adapter, authenticated owner memory/report endpoints, actual Main/Instore UI instrumentation, isolated full-app auth setup, scheduled retention and coverage monitoring, Positill invoice/credit/tax verification/reconciliation, end-to-end browser tests, live release gates. Do not enable collection until retention and identity flows are integrated.

### 23:28 SAST — Apollo integration contracts, still feature-disabled

- Two lower-cost agents built narrow pure adapters; independent review audited current Positill catalogue without querying Positill. Parent rejected/corrected initial adapter mismatches before accepting. Added strict server-side activity event adapter: verified server customer UUID only, no browser customer identity, explicit event/source/payload schema, canonical timestamps, replay-safe event IDs and private table row shape. Active intervals now live in payload JSON exactly as the migration expects; a strict reader maps only valid active payloads back into overlap-safe reporting intervals.
- Added strict memory-store adapter: browser payload cannot contain reviewer/version; authenticated server reviewer UUID and expected optimistic version become the exact stored RPC arguments. Database rows map only reviewed fields into full-history, fail-closed memory retrieval validation.
- Verified a synthetic active event insert against the isolated branch table in a transaction and rolled it back. This confirms the activity adapter shape matches the private table. No collection endpoint, customer browser wiring, Vercel environment change, deployment or live data write exists yet.
- Positill evidence audit: current read-only Python catalogue joins DBINVHD/DBINVDT by composite INV_NO+TYPE but exposes invoice lines without TYPE, tax, customer/external reference or source timezone. Credits are only negative-line heuristics. Tax basis, document signs/type mapping and website-to-POS references remain unverified, so Apollo continues to show POS/reconciliation unavailable rather than calculate a misleading figure. Do not reuse donor _sql-reports.js: it is parameter/schema incompatible with current catalogue.
- Verification: 62 focused Apollo tests passed (reporting, API, UI, memory, memory store, activity union and activity ingest). No production deployment. Candidate recovery manifest rechecked after additions separately.

### 23:35 SAST — Disabled owner-memory route

- Added api/apollo-memory.js with a default-off APOLLO_MEMORY_ENABLED gate. It requires a verified owner JWT before initialising any client, uses only dedicated APOLLO_MEMORY_SUPABASE_URL / APOLLO_MEMORY_SUPABASE_SERVICE_ROLE_KEY (never the portal database fallback), reads the exact private revision table and invokes only the expected-version append RPC. Browser reviewer identity and version are rejected; reviewer derives from the verified server user.
- The endpoint returns only allowlisted reviewed memory content, generic errors and private no-store responses. It is not deployed and neither flag nor database credentials have been configured in Vercel.
- Verification: focused owner-memory API, storage-contract and memory-history tests passed. No production schema, environment, customer data or deployment changed.

### 23:44 SAST — Read-only Positill bridge hardening and invoice schema gate

- Hardened only the candidate bridge source: server mode now requires a non-empty STOCK_SQL_BRIDGE_KEY before it binds, compares supplied keys in constant time and fails closed on missing/wrong keys. Unexpected bridge errors now log only their exception class locally and return a fixed redacted response. Unsupported routes remain 404 before authentication; CLI catalogue listing remains available without SQL connection.
- Added system.invoice_schema to the fixed v4.3 allowlist. It accepts no parameters and queries only INFORMATION_SCHEMA.COLUMNS metadata for dbo.DBINVHD and dbo.DBINVDT (table, column, datatype, nullability and ordinal position). It does not retrieve invoice lines, customer data, VAT, stock, prices or business values, and makes no inference about tax, credits or website links.
- Bridge report audit recorded: invoice lines currently omit document TYPE and there is no verified tax basis, credit-note sign convention, database timezone or explicit website-order reference. Apollo retains Positill/reconciliation as unavailable. Existing _sql-sales aggregate is not used by Apollo and must not be labelled reconciled revenue until those conventions are verified.
- Verification: C:/Python314/python.exe scripts/test_sql_bridge_json.py with bytecode disabled: 17 passed, 1 live-SQL test skipped because no configured SQL runtime was used. No Positill connection, process restart, tunnel change, Vercel setting or production deployment occurred. Candidate recovery manifest passed with no protected-file deletion or out-of-scope change.
- Next gate: the running port-8765 bridge predates this candidate and its source/version parity is unknown. Do not replace or restart it until a separate local deployment/restart approval, current-process preflight, and post-start authenticated read-only health check are complete.

### 23:50 SAST — Apollo contract review correction

- Independent review confirmed that the Apollo memory RPC and its grant both use the same eight-argument signature; the initially reported grant mismatch was a counting error, so the applied preview migration was not changed.
- Corrected the storefront activity API's client-side acceptance window from 366 days to one day to match the private database constraint. Added an API boundary test proving an older event is rejected before a database client is created.
- Added a successful owner-memory read test that proves a newer unreviewed draft suppresses an older approved revision instead of exposing stale approved memory as current. Feature flags remain disabled and no preview or production configuration changed.

### 00:05 SAST — Disabled Apollo activity-retention executor

- Added the isolated-candidate-only `api/apollo-activity-retention.js` path. It is off unless `APOLLO_ACTIVITY_RETENTION_ENABLED=true`, accepts only an empty POST, and invokes only the service-role-only `apollo_retain_activity` RPC already verified in the Apollo test branch.
- It uses only the dedicated Apollo activity database variables, never a portal fallback or browser variable. Execution requires a verified owner session or a distinct `APOLLO_ACTIVITY_RETENTION_CRON_SECRET`; it deliberately does not reuse the broader site cron credential.
- No Vercel cron entry has been added. Scheduling and its isolated preview environment variables remain an explicit release gate after the endpoint and data-retention job have been exercised against the test branch.
# Apollo memory read pagination

- Added a candidate-only, service-role-only `apollo_read_memory` function. It pages stable memory keys (1–100 per request) but returns each selected key's full revision history, preserving fail-closed approved-only retrieval.
- The owner-only memory API now validates a bounded key cursor/page size before creating a database client, returns `nextCursor`, and no longer loads the entire memory history for every read.
- The request-size guard now accommodates a valid, fully bounded 10,000-character decision plus its allowed evidence references; the database and contract remain the authoritative per-field limits.
- No Supabase migration was applied and no deployment or environment setting was changed.

### 00:09 SAST — Memory pagination schema verified in the Apollo test database

- Applied only `apollo_memory_read_pagination` to the explicitly approved isolated Apollo test database `ppnlkkvdouxsftscdpei`. No production database, portal database, customer record or environment variable changed.
- Verified the function is `SECURITY INVOKER`, executable by `service_role`, and not executable by `anon` or `authenticated`. It remains a server-only owner-memory read primitive; the API flag is still disabled and no hosted preview is configured.

### 00:33 SAST — Rolled-back Apollo database function checks

- In the isolated Apollo test database only, a transaction inserted then read a reviewed memory revision through `apollo_append_memory` and `apollo_read_memory`; a separate transaction inserted an event older than 90 days, ran `apollo_retain_activity`, and verified that event was removed from the raw table. Both transactions rolled back.
- Follow-up checks confirm zero temporary memory rows and zero temporary activity rows remain. No real customer, sales, production database or hosted environment was accessed or changed.

### 00:39 SAST — Release authority recorded

- Added `md/apollo-release-gates.md` to the isolated candidate. It names the evidence required for identity, activity, reporting, Positill semantics, reconciliation, preview, deployment and rollback. It explicitly prevents unavailable feeds from being displayed as zero.

### 07:20 SAST — Existing bridge retained

- Verified in the authorised Cloudflare dashboard that the healthy `hermes-proto-bridge1` tunnel (BladeRunner-PC connector) publishes `sql-bridge.proto.co.za` to `http://localhost:8765`. Apollo will retain this established bridge and use its existing authenticated, read-only interface; no bridge, tunnel, DNS, SQL Server or Positill change is required.
- Updated the release gate to require evidence from existing approved reports only. The browser blocks direct navigation to the admin API route before it reaches the server, so that client-side block is not treated as a bridge health result.

### 09:10 SAST — Owner-only activity reporting reader

- Added a candidate-only service-role activity reader for the dedicated Apollo database. It exposes per-source collection status, last successful collection, freshness, verified coverage, malformed-row counts and estimated active time without treating missing health or collection gaps as zero.
- Active intervals are clipped and unioned across overlapping browser tabs and Main/Instore surfaces. The reader fetches the one-minute left overlap required for intervals crossing a reporting boundary, while malformed events remain visible as an incomplete feed.
- No environment variable, client fallback, portal database access, Vercel configuration, preview deployment or production collection switch was added. The reader is not yet wired into the enabled pulse API.

### 00:16 SAST — Feature-disabled existing-bridge Positill evidence adapter

- Apollo can now call only the existing authenticated, read-only `top-sellers` bridge report when `APOLLO_POSITILL_EVIDENCE_ENABLED=true`. The switch is separate from the Apollo Pulse switch and remains off by default.
- The adapter deliberately accepts only the current-day Apollo window: the existing bridge has named rolling periods and cannot represent Apollo's calendar week, month or custom boundaries. It returns raw line aggregates as supporting evidence only, with explicit unknown VAT basis, credit treatment, document type convention and website reconciliation. It does not calculate, label or combine any value as verified sales or revenue.
- Verification: 13 focused adapter/Pulse tests and changed-file lint passed. No bridge, Cloudflare tunnel, SQL Server, Positill, Vercel secret, preview or production deployment was changed.

### 00:24 SAST — Activity reporting wired to the isolated Apollo source

- Wired the owner-only Pulse summary to the dedicated Apollo activity reader. It creates a client only from `APOLLO_ACTIVITY_SUPABASE_URL` and `APOLLO_ACTIVITY_SUPABASE_SERVICE_ROLE_KEY`; it never falls back to the portal database. Missing keys, a missing migration or incomplete source health return an explicit unavailable/partial source rather than a zero duration.
- Kept collection and retention feature switches off. This only makes already-collected, coverage-labelled preview data readable after the isolated preview variables are configured; it does not add tracking, a scheduler, bridge access or production data changes.
- Corrected the activity ingest event-age gate to one day, matching the database constraint. A new regression proves an event older than that boundary is rejected before a database insert is attempted.
- Verification: 28 focused Pulse/activity/Positill tests and changed-file lint passed; admin production build passed. The recovery manifest passed with no deleted live file and no change outside the approved allowlist. No remote branch, preview deployment, Vercel configuration, Cloudflare route, SQL bridge or production system was changed.
