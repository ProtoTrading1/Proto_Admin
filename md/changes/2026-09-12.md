# 2026-09-12

## Instore listing removal control prepared

- Added a separate, reversible owner-only Instore listing control. It is deliberately distinct from photo hiding: an image hide preserves sale eligibility, while a listing hide removes the exact SKU from Instore discovery and checkout until restored.
- The pending migration `069_instore_listing_controls.sql` adds private, RLS-protected control and audit tables plus a service-role-only `SECURITY INVOKER` RPC. It does not update product, stock, price, image, or main-catalogue records.
- Applied to the Proto Stock Tracking Supabase database after explicit authorization. Verification found zero control records; anon cannot read or execute the RPC, while service_role can.

## 09:25 — Instore image-only safety controls (preview branch)

- Added an owner-only Image Processing Centre workflow that hides or restores an incorrect Instore product photo by exact SKU.
- Added the pending stock-database migration `068_instore_image_controls.sql`: private control and event tables plus a service-role-only, atomic audit RPC. It does not modify product records, prices, stock, catalogue classification, or ordering eligibility.
- Affected systems: admin API and owner UI; the customer storefront branch reads the new control state and presents an “Image being updated” placeholder while retaining checkout eligibility.
- Verification: admin lint and production build passed. The focused Vitest command is blocked locally by the worktree's esbuild config-loader permission error; the test source is committed for CI.
- Deployment: application changes not deployed; the database migration is recorded below; commit `3d91c28` was amended to record this journal update.

## 09:42 — Applied private Instore image-control schema

- Applied migration `068_instore_image_controls.sql` to the Proto Stock Tracking Supabase database (`yiqsvwajozafvalwcero`).
- Verified both new tables exist; `anon` cannot read the control table or execute the update function, while `service_role` can. No control records, product records, prices, stock values, or images were changed.
- Deployment: application branches remain preview-only; commit reference pending this journal update.
