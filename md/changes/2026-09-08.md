# 2026-09-08

## 10:20 — School customer badge in Customer Management

**Summary.** Schools now register on a new standalone site
(`schools/` in ProtoMainSite, deployed as its own Vercel project). Those
accounts land in the same `customers` table flagged `is_school`, and the admin
dashboard badges and explains them.

**Files affected.**
- `src/pages/AdminPage.jsx` — new `SchoolBadge` component (blue pill, mirrors
  `TenThousandClubBadge`), rendered in both customer lists and the customer
  profile header. Falls back to the `School, church or institution` sales
  channel so rows written before the migration are still badged.
- `src/components/CustomerApplicationDetails.jsx` — school rows ("Role at the
  school", "Supply needs"); the panel kicker reads "School registration" for a
  school instead of "Online trade application".
- `api/admin-customers.js` — `is_school`, `school_role` and `supply_needs`
  added to the PATCH allowlist so an admin can correct them. The list endpoint
  already selects `*`, so no read change was needed.
- `migrations/066_school_signup.sql` — new. Adds `is_school boolean not null
  default false`, `school_role text`, `supply_needs text[]`, a cardinality
  check and a partial index on `is_school`. Identical to migration 067 in
  ProtoMainSite (same table, both repos keep their own copy).
- `tests/school-customer-badge.test.js` — new, 6 tests.

**Rules kept.** Schools are never auto-approved and never get an
auto-generated customer code — both stay manual admin steps, as for any trade
application. No customer WhatsApp messaging was added.

**Verification.**
- `npm run lint` — clean.
- `npx vitest run` — 804 passed, 1 failed.
  The failure is **pre-existing and unrelated**:
  `tests/email-campaign-follow-up.test.js` expects
  `label: 'Bounced / excluded'` in `EmailAnalyticsPanel.jsx`, which currently
  reads `label: 'Bounced'`. None of those files are in this change; the test
  and the panel had already drifted apart on `main`.
- `npx vite build` — succeeded.

**Database status.** Migration 066 **applied** to the live `Proto Trading
Website` Supabase project (ref kyodrsqnmihwoplkhwwf) at 10:34 via the Supabase
MCP. (Migration 067 in ProtoMainSite is the same DDL on the same table — it was
applied once, not twice.) Verified after applying: `is_school` (boolean, not
null, default false), `school_role` (text), `supply_needs` (text[]), the
`customers_supply_needs_count` constraint and the `customers_is_school_idx`
partial index all present; 0 rows currently flagged as schools. Additive only —
no existing data was rewritten or dropped.

**Deployment status.** Merged to `main` (8a0a433) and pushed, which triggers the
existing Vercel Git integration for `protoportal-admin` — the SCHOOL badge goes
live on admin.proto.co.za on that deploy. Not verified in production from here;
this environment cannot reach Vercel.

**Commits.** `6fc71a0` on `claude/unspecified-task-ajgzgb`, merged as `8a0a433`
on `main`.

## 11:15 — Public / private school in the admin view

**Summary.** Schools now declare Public vs Private at registration (see
ProtoMainSite, same date). Surface it in the admin.

**Files affected.**
- `src/components/CustomerApplicationDetails.jsx` — "School type" row, falling
  back to `business_type` (which carries the same label) when `school_type` is
  not set.
- `src/pages/AdminPage.jsx` — the SCHOOL badge tooltip now reads e.g.
  "Registered on the school-supply site — Private school · Procurement officer".
- `api/admin-customers.js` — `school_type` added to the PATCH allowlist.
- `migrations/067_school_type.sql` — new. `school_type text` plus a check
  constraint limiting it to 'Public school' / 'Private school'. Same DDL as
  migration 068 in ProtoMainSite; applied once, not twice.
- `tests/school-customer-badge.test.js` — new public/private test.

**Note.** `business_type` is set to the same label at registration, so the
existing Customer Management business-type filter separates public from private
schools with no further change.

**Verification.**
- `npm run lint` — clean.
- `npx vitest run` — school badge suite 7/7 passing; full run below.
- `npx vite build` — succeeded.

**Database status.** Migration 067 **applied** to the live `Proto Trading
Website` project (ref kyodrsqnmihwoplkhwwf) via the Supabase MCP. Additive only.

**Deployment status.** Pushed to `main`; the existing Vercel Git integration
deploys `protoportal-admin` from it.
