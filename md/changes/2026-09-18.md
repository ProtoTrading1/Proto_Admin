# 2026-09-18

## 20:45 — Protected Instore Products review queue (test-only)

- Added an owner-only `Instore Products` review section, private intake image staging, separate Live/Archive/Recycle lifecycle, staff-confirmed physical-stock mode, and event-backed optimistic concurrency.
- Added `api/instore-admin.js`, its preview-only safety guard, client API, review UI, and focused tests. Existing Product Loader Image Processing and Instore image/listing controls remain unchanged.
- Existing Positill/extended-range SKUs are treated as the source for the private review overlay rather than rejected as catalogue duplicates. The endpoint stores a source snapshot and does not update catalogue, product, stock, archive, image-control, or listing-control records.
- A disposable Supabase branch was used for schema/lifecycle proof only; no production database migration, production Vercel configuration, deployment, or catalogue data change was made.
- Verification: focused Instore test suite and build pending final branch run. Publish/approval stays disabled until a separately tested storefront and checkout contract exists.
- Branch: `codex/instore-admin-review-20260918`; no commit or pull request yet.
